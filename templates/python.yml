name: my-app

rows:
  - size: 70%
    panes:
      - title: Claude 1 — feature
        command: claude
      - title: Claude 2 — review
        command: claude

  - panes:
      - title: Server
        # command: uvicorn main:app --reload
      - title: Shell
        focus: true
